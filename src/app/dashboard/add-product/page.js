import React from 'react';

export const metadata = {
    title: 'Add Product | Dashboard | Grocery Shop',
    description: 'Grocery Shop',
}

const AddProduct = () => {
    return (
        <div>
            Add Product Content
        </div>
    );
};

export default AddProduct;

